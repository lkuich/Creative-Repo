const express = require("express");
// const knex = require("./db");

const router = express.Router();

/*
router.post("/myAction", async (req, res) => {
  const { id, otherInput } = req.body.input;

  const { email } = res.locals.token;

  const result = ...;

  res.json(result);
});
*/

module.exports = router;
